# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/

# sleepctl runtime artifacts
*.db
*.db-wal
*.db-shm
raw_capture.jsonl
token.json
credentials.json
sleepctl.egg-info/

# session preview artifacts (not part of the project)
scratch_data/

# codespace-up.sh runtime logs
.run/

# Claude Code agent worktrees (harness scratch; never commit)
.claude/
# deploy env files hold real secrets (DASHBOARD_PASSWORD, JWT_SECRET, VAPID keys, Eight
# Sleep creds) once `make up`/the operator fills them in — only .env.example is tracked.
deploy/.env
.env

# Downloaded / reduced research datasets (PhysioNet DREAMT, NSRR MESA, BIDSleep) and their
# training caches. DREAMT and MESA are data-use-agreement data: they must never be committed.
cache/
