services:

  # ── FastAPI backend ──────────────────────────────────────────────────────────
  api:
    build:
      context: ..          # repo root — Dockerfile COPYs the whole tree
      dockerfile: dashboard/api/Dockerfile
    image: sleepctl-api
    env_file: .env
    environment:
      SLEEPCTL_DB: /data/sleepctl.db
      PYTHONPATH: /app:/app/dashboard/api
      # Token-less phone-sensor ingest on the LAN (the two /bcg/* endpoints only).
      # Defaults ON for this single-user home setup; set BCG_INGEST_OPEN=0 in .env
      # to re-lock (e.g. if you ever expose the API to the internet).
      BCG_INGEST_OPEN: "${BCG_INGEST_OPEN:-1}"
    volumes:
      - sleepdata:/data
    restart: unless-stopped
    # Published so an iPhone can stream straight to the API at http://<server-ip>:8000.
    # (Caddy still fronts the dashboard on 80/443; this is the LAN ingest path.)
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python3", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ── Control daemon (same image, different CMD) ───────────────────────────────
  daemon:
    image: sleepctl-api          # reuse the image built above
    depends_on:
      api:
        condition: service_started
    command: ["python", "/app/dashboard/daemon/run_daemon.py"]
    env_file: .env
    environment:
      SLEEPCTL_DB: /data/sleepctl.db
      PYTHONPATH: /app:/app/dashboard/api
    volumes:
      - sleepdata:/data
    restart: unless-stopped

  # ── Next.js PWA frontend ─────────────────────────────────────────────────────
  web:
    build:
      context: ..
      dockerfile: dashboard/web/Dockerfile
    image: sleepctl-web
    environment:
      # Used by Next.js server-side rewrites / middleware to reach the API
      API_URL: http://api:8000
      NEXT_PUBLIC_API_URL: ""   # empty → browser uses relative /api/* paths via Caddy
      PORT: "3000"
    depends_on:
      - api
    restart: unless-stopped

  # ── Caddy reverse proxy (HTTPS + PWA host) ───────────────────────────────────
  caddy:
    image: caddy:2
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api
      - web
    restart: unless-stopped

volumes:
  sleepdata:
  caddy_data:
  caddy_config:
