"""Targeted, standalone analyses over the sleepctl dataset (read-only).

Distinct from ``sleepctl.eval`` (the efficacy trial): this package answers focused
"what pattern shows up in MY data" questions -- the first is the 3AM WAKE targeted
analysis in ``wake_patterns``.
"""
